package com.phonewhisperer.presentation.theme

import androidx.compose.ui.graphics.Color

/**
 * PhoneWhisperer color palette.
 *
 * Uses a dark-mode-first approach with deep indigo/purple accents
 * that evoke "AI intelligence" without being aggressive.
 */

// ── Primary (Deep Indigo) ───────────────────────────────────────
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650A4)
val PurpleGrey40 = Color(0xFF625B71)
val Pink40 = Color(0xFF7D5260)

// ── PhoneWhisperer Brand Colors ─────────────────────────────────
val WhispererPrimary = Color(0xFF7C4DFF)       // Vibrant indigo
val WhispererPrimaryVariant = Color(0xFF651FFF) // Deeper indigo
val WhispererSecondary = Color(0xFF00E5FF)      // Cyan accent
val WhispererTertiary = Color(0xFFFF6E40)       // Warm orange for alerts
val WhispererAccent = Color(0xFFE040FB)         // Vibrant magenta for notifications

// ── Surface / Background ────────────────────────────────────────
val DarkBackground = Color(0xFF0D1117)          // GitHub-dark inspired
val DarkSurface = Color(0xFF161B22)
val DarkSurfaceVariant = Color(0xFF21262D)
val DarkCard = Color(0xFF1C2128)

// ── Status Colors ───────────────────────────────────────────────
val StatusActive = Color(0xFF00E676)            // Green — collecting data
val StatusPaused = Color(0xFFFFAB00)            // Amber — paused
val StatusWarning = Color(0xFFFF9100)           // Deep orange — warning
val StatusError = Color(0xFFFF5252)             // Red — error
val StatusProcessed = Color(0xFF7C4DFF)         // Purple — processed by AI

// ── Text ────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFE6EDF3)
val TextSecondary = Color(0xFF8B949E)
val TextMuted = Color(0xFF484F58)
